import AuthForm from "../components/AuthForm.js";

const Login = () => {
  return (
    <div className="h-screen bg-black">
      <AuthForm type={"Login"} />
    </div>
  );
};

export default Login;
